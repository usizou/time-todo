# Time & To-Do

タイマー機能を中心にしたデスクトップアプリです（[Electron](https://www.electronjs.org/) 製）。
将来的に To-Do 機能を追加していく予定です。

## 機能

### カウントダウンタイマー
- 最大 **12 時間**、時 / 分 / 秒で自由に設定
- プリセット（1分・5分・10分・25分・1時間・3時間）
- **円形リング**で残り時間を視覚化（残り10秒で赤く警告）
- 終了時にビープ音と OS 通知

### ポモドーロタイマー
- 作業25分 → 小休憩5分 →（4回ごとに）長休憩15分 を自動で巡回
- フェーズ切替時に音と通知、完了セッション数を表示

### 共通
- スペースキーでスタート / 一時停止
- 上部タブで「タイマー / To-Do」を切替（To-Do は今後実装予定）

## 開発

必要環境: [Node.js](https://nodejs.org/)（18以上を推奨）

```bash
# 依存関係のインストール
npm install

# 開発起動
npm start
```

## ビルド（Windows exe の作成）

```bash
npm run dist
```

`dist/` に以下が生成されます:

- `TimeToDo-portable-<version>.exe` … ポータブル版（インストール不要でダブルクリック起動）
- `Time & To-Do Setup <version>.exe` … インストーラー版

> 署名は行っていないため、初回起動時に Windows SmartScreen の警告が出ることがあります。
> その場合は「詳細情報」→「実行」で起動できます。

## 構成

| ファイル | 役割 |
|---|---|
| `main.js` | アプリ本体・ウィンドウ・OS通知・データ保存 |
| `preload.js` | 画面（レンダラー）と本体の安全な橋渡し |
| `src/index.html` | 画面レイアウト（タブ構成） |
| `src/renderer.js` | タイマーの動作ロジック |
| `src/style.css` | 見た目 |

## ライセンス

[MIT](./LICENSE)
